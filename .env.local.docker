# Configuration pour Docker Compose
APP_ENV=dev
APP_SECRET=change-me-to-random-secret-key

# Base de données MySQL (Docker)
DATABASE_URL="mysql://symfony:symfony_password@mysql:3306/symfony_db?serverVersion=8.0&charset=utf8mb4"

# Alternative si vous utilisez directement depuis l'hôte (pas dans le container)
# DATABASE_URL="mysql://symfony:symfony_password@127.0.0.1:3306/symfony_db?serverVersion=8.0&charset=utf8mb4"
